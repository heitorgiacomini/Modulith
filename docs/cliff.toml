# git-cliff ~ configuration file
# https://git-cliff.org/docs/configuration

[changelog]
# A Tera template to be rendered as the changelog's header.
# See https://keats.github.io/tera/docs/#introduction
header = """
"""
# A Tera template to be rendered for each release in the changelog.
# See https://keats.github.io/tera/docs/#introduction
body = """
{%- macro remote_url() -%}
  https://github.com/heitorgiacomini/Modulith
{%- endmacro -%}

{% macro commit_author(commit) -%}
{%- if commit.author.name is defined and commit.author.name -%}
{{ commit.author.name }}
{%- elif commit.author is defined and commit.author -%}
{{ commit.author }}
{%- else -%}
unknown
{%- endif -%}
{%- endmacro %}

{% macro print_commit(commit) -%}
    - {% if commit.scope %}*({{ commit.scope }})* {% endif %}\
        {% if commit.breaking %}[**breaking**] {% endif %}\
  {{ commit.message | upper_first }} ({{ self::commit_author(commit=commit) }}) - \
        ([{{ commit.id | truncate(length=7, end="") }}]({{ self::remote_url() }}/commit/{{ commit.id }}))\
{% endmacro -%}

{% if version %}\
    {% if previous.version %}\
        ## [{{ version | trim_start_matches(pat="v") }}]\
          ({{ self::remote_url() }}/compare/{{ previous.version }}..{{ version }}) - {{ timestamp | date(format="%Y-%m-%d") }}
    {% else %}\
        ## [{{ version | trim_start_matches(pat="v") }}] - {{ timestamp | date(format="%Y-%m-%d") }}
    {% endif %}\
{% else %}\
    ## [unreleased]
{% endif %}\

{% set_global current_day = "" %}
{% set_global current_group = "" %}

{% for commit in commits | sort(attribute="committer.timestamp") | reverse %}
  {% set day = commit.committer.timestamp | date(format="%Y-%m-%d") %}
  {% if day != current_day %}
    {% set_global current_day = day %}
    {% set_global current_group = "" %}
    ### {{ day }}
  {% endif %}

  {% if commit.group != current_group %}
    {% set_global current_group = commit.group %}
    #### {{ commit.group | striptags | trim | upper_first }}
  {% endif %}

  {{ self::print_commit(commit=commit) }}
{% endfor -%}


"""
# A Tera template to be rendered as the changelog's footer.
# See https://keats.github.io/tera/docs/#introduction
footer = """
<!-- generated by git-cliff -->
"""
# Remove leading and trailing whitespaces from the changelog's body.
trim = true
# An array of regex based postprocessors to modify the changelog.
postprocessors = [
  # Replace the placeholder `<REPO>` with a URL.
  { pattern = '<REPO>', replace = "https://github.com/heitorgiacomini/Modulith" }, # replace repository URL
]

[git]
# Parse commits according to the conventional commits specification.
# See https://www.conventionalcommits.org
conventional_commits = true
# Exclude commits that do not match the conventional commits specification.
filter_unconventional = false
# Split commits on newlines, treating each line as an individual commit.
split_commits = false
# An array of regex based parsers to modify commit messages prior to further processing.
commit_preprocessors = [
  # Replace issue numbers with link templates to be updated in `changelog.postprocessors`.
  { pattern = '\((\w+\s)?#([0-9]+)\)', replace = "([#${2}](<REPO>/issues/${2}))" },
  # Optional: spellcheck commit messages using https://github.com/crate-ci/typos
  # Enable only if you have `typos` installed and on PATH.
  # { pattern = '.*', replace_command = 'typos --write-changes -' },
]
# An array of regex based parsers for extracting data from the commit message.
# Assigns commits to groups.
# Optionally sets the commit's scope and can decide to exclude commits from further processing.
commit_parsers = [
  { message = "(?i)^feat", group = "<!-- 0 -->⛰️  Features" },
  { message = "(?i)^fix", group = "<!-- 1 -->🐛 Bug Fixes" },
  { message = "(?i)^doc", group = "<!-- 3 -->📚 Documentation" },
  { message = "(?i)^perf", group = "<!-- 4 -->⚡ Performance" },
  { message = "^refactor\\(clippy\\)", skip = true },
  { message = "(?i)^refactor", group = "<!-- 2 -->🚜 Refactor" },
  { message = "(?i)^style", group = "<!-- 5 -->🎨 Styling" },
  { message = "(?i)^test", group = "<!-- 6 -->🧪 Testing" },
  { message = "^chore\\(release\\): prepare for", skip = true },
  { message = "^chore\\(deps.*\\)", skip = true },
  { message = "^chore\\(pr\\)", skip = true },
  { message = "^chore\\(pull\\)", skip = true },
  { message = "^chore\\(npm\\).*yarn\\.lock", skip = true },
  { message = "(?i)^chore|(?i)^ci", group = "<!-- 7 -->⚙️ Miscellaneous Tasks" },
  { body = ".*security", group = "<!-- 8 -->🛡️ Security" },
  { message = "(?i)^revert", group = "<!-- 9 -->◀️ Revert" },
  # Catch-all so non-conventional messages still appear in the changelog.
  { message = ".*", group = "<!-- 99 -->🧱 Other" },
]
# Prevent commits that are breaking from being excluded by commit parsers.
protect_breaking_commits = false
# Exclude commits that are not matched by any commit parser.
filter_commits = false
# Regex to select git tags that represent releases.
tag_pattern = "v[0-9].*"
# Regex to select git tags that do not represent proper releases.
# Takes precedence over `tag_pattern`.
# Changes belonging to these releases will be included in the next release.
skip_tags = "beta|alpha"
# Regex to exclude git tags after applying the tag_pattern.
ignore_tags = "rc|v2.1.0|v2.1.1"
# Order releases topologically instead of chronologically.
topo_order = false
# Order of commits in each group/release within the changelog.
# Allowed values: newest, oldest
sort_commits = "newest"